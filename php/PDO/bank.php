<?php

/**
 *   create database exemplo;
 *   use exemplo;
 *
 *   create table product (
 *       cd_product int not null auto_increment,
 *       nm_product varchar(50) default null,
 *       constraint pk_product primary key(cd_product)
 *   );
 * 
 *  insert into product values (default, "Coxão");
 *  insert into product values (default, "Cama");
 *  insert into product values (default, "Sofá");
 *   
 */