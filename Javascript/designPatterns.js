/** Factory
 * Todas as funções que retornam um objeto, sem a necessidade de
 * chamá-las com o new, são consideradas funções Factory(); 
 */

/** Singleton
 * O obejetivo desse pattern é criar uma única instância de uma função
 * construtora e retorná-la toda vez em que for necessário utilizá-la.
 */

/** Decorator
 * Uma função decorator recebe uma outra função como parâmetro e 
 * estende o seu comportamento sem modificá-la explicitamente
 */

/** Observer
 * É um pattern muito popular em aplicações javascript. A instância
 * (subscriber) mantém uma celeção de objetos (obervers) e notifica todos
 * eles qunaod ocorrem mudanças no estado.
 */

/** Module
 * É um pattern que possibilita organizamos melhor o nosso código, sem a 
 * necessidade de expor variáveis globais.
 */

